# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## IMPORTANT: Codebase Intelligence Instructions for shame

> This repository is indexed by [Repowise](https://repowise.dev).
> Use the MCP tools below for orientation, discovery, and enriched context
> (documentation, ownership, history, decisions). **Always verify against
> actual source files before making changes** — the index may be stale.

Last indexed: 2026-05-28 (commit 1579160). Confidence: 100%.
### Architecture
This repository is a Dota 2 post-match dashboard: it consumes raw match data exported from the OpenDota API, transforms it through a data-loading pipeline and a Cloudflare Worker backend, and produces an interactive web UI that renders squad performance stats, match autopsies, shame metrics, and admin controls. The system is split into two deployable units — a static React frontend (served via Cloudflare Pages) and a Cloudflare Worker API — that together turn structured JSON match data into a browsable, shareable game-recap experience. A shell script (update.sh) drives the data refresh cycle, pulling new match data and committing updated JSON fixtures that the frontend consumes. ---



| Layer | Technology | Notes |
|---|---|---|
| Frontend UI | React (JSX, no build step / lightweight bundler) | Component files in public/components/ |
| Backend API | Cloudflare Workers (JavaScript) | Entry: worker/src/index.js |
| Frontend Hosting | Cloudflare Pages | Configured via public/wrangler.toml |
| Worker Config | Wrangler (TOML) | worker/wrangler.toml |
| Data Format | JSON fixtures | public/data/*.json |
| Type Declarations | TypeScript (.d.ts only) | public/worker-configuration.d.ts |
| Data Pipeline | Shell scripting | update.sh orchestrates OpenDota → JSON |
| Documentation | Markdown | CLAUDE.md, DEPLOY.md, START_HERE.md, tools/, docs/ |

**Language split:** JavaScript dominates at ~44% (UI + Worker logic), with JSON at ~17% (data fixtures), Markdown at ~22% (docs/tooling guides), TOML at ~9% (deployment config), and a small TypeScript surface for type declarations.
### Entry Points
- `public/app.jsx`
- `worker/src/index.js`
### Architectural Layers
| Layer | Files | Purpose |
|-------|-------|---------|
| Data Loading Layer | 1 | Handles fetching and loading data for the frontend application. |
| Frontend App Core | 5 | Contains the main application entry point, configuration, UI panels, and fallbac |
| Deployment Documentation | 1 | Provides instructions and guidance for deploying the application. |
| Autopsy UI Component | 1 | A standalone UI component responsible for rendering autopsy-related views. |
| Static Content Data | 1 | Stores static JSON data assets used by the application. |
| Claude AI Docs | 1 | Documentation file providing guidance or context specifically for Claude AI inte |
| Onboarding Guide | 1 | Entry-point documentation file directing new developers on where to begin with t |
| Update Script | 1 | Shell script for automating project update or deployment procedures. |
| Workers Routing Docs | 1 | Documentation describing the routing architecture and conventions for the worker |
| UI Page Components | 4 | Frontend React components covering admin, pages, shared utilities, and squad-rel |

### Guided Tour (11 steps)
1. **Start Here: Project Overview** — `START_HERE.md`
2. **Frontend Entry Point** — `public/app.jsx`
3. **Static Data Assets** — `public/data/squad.json`
4. **Data Loading Layer** — `public/data-loader.js`
5. **Core UI Components** — `public/components/shared.jsx`
6. **Page and Admin Components** — `public/components/pages.jsx`
... and 5 more steps
### Hotspots (High Churn)
| File | Churn | 90d Commits | Owner |
|------|-------|-------------|-------|
| `public/worker-configuration.d.ts` | 94.7th %ile | 1 | skogix |
| `public/components/autopsy.jsx` | 89.5th %ile | 1 | skogix |
| `public/components/admin.jsx` | 84.2th %ile | 1 | skogix |
| `public/tweaks-panel.jsx` | 78.9th %ile | 1 | skogix |

## Code health
Hotspot health: 8.36/10 (stable) ·
Average: 8.56/10 ·
Worst: 7.8/10 (`public/components/autopsy.jsx`)

### Critical biomarkers
- `public/components/admin.jsx` — complex method (AdminPanel) — impact −0.7
- `public/components/autopsy.jsx` — complex method (MatchTimeline) — impact −0.6

### Repowise MCP Tools

This repo has the Repowise MCP server configured. The tools below answer questions `grep`/`Read` cannot. Every response carries an `_meta` envelope with `index_age_days`, `indexed_commit`, and a `stale_warning` only when the index has actually diverged from HEAD — silence means the index is current.

**When to call which tool:**

| Tool | What only this tool answers |
|------|------------------------------|
| `get_answer(question)` | Synthesised answer with verified citations and a calibrated `retrieval_quality`. First call for "how does X work" / "why is Y like this". On low confidence returns `best_guesses` with one-line justifications instead of an empty answer. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols — title, summary, signatures, `hotspot` bit, `decision_records` titles, and `symbol_id`s to pipe into `get_symbol`. Use `include=["callers","ownership",...]` to widen. NOT for source bytes. |
| `get_symbol("path/to/file.py::Name")` | Raw source bytes for one indexed symbol with exact line bounds. Cheaper and safer than `Read` + offset math. Use the `symbol_id` returned by `get_context`. |
| `search_codebase(query, kind?)` | Find pages by concept when you don't know the file. Each result carries `search_method` (`embedding` vs `bm25` fallback). For exact identifiers use Grep — the tool will hint when it sees one. |
| `get_why(query, targets?)` | Architectural decision archaeology — *why* the code is shaped this way. Call before refactors or pattern divergences. Falls back to git archaeology when no ADRs exist for a file. |
| `get_risk(targets, changed_files?)` | What history says about touching these files: churn, owners, blast radius. Pass `changed_files` for PR mode → returns a `directive` (`will_break`, `missing_cochanges`, `missing_tests`). |
| `get_dead_code(...)` | Tiered unreachable / unused-export / zombie-package findings. Run before a cleanup sprint, not before a targeted fix. |
| `get_overview(repo?)` | Architecture map for an unfamiliar repo. One-time orientation; skip on subsequent calls in the same session. |

**Composition tips:**
- `get_answer` → if `confidence` is `medium`/`low`, follow the `best_guesses[0].file` or `fallback_targets[0]` into `get_context`, then `get_symbol` for bytes.
- `get_context` returns `decision_records` titles → call `get_why(targets=[...])` for the rationale.
- `get_context` returns `hotspot: true` → call `get_risk` before editing.
- PR review → `get_risk(targets=[...], changed_files=[...])`; read the `directive` block first.

**Verify when:** `_meta.stale_warning` is present, or `retrieval_quality` is `partial`/`weak`, or `search_method` is `bm25`. Otherwise trust the response and act on it.

<!-- REPOWISE:END -->
